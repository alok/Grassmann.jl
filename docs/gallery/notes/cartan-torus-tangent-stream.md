The streamlines are traced in parameter space and pushed through the embedding (Makie's `transform_func`); they agree bit for bit. Julia draws in the automatic `LScene` (an `Axis3` loses the transformed limits in CairoMakie), Lean in an `Axis3` with data aspect, so the camera differs. The arrowhead size uses the triangulated surface area instead of Cartan's `surfacearea` (a differential-geometry integral).
