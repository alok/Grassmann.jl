2026-07-09 INITIAL RECOVERY

- Read repository-local Lean 4 guidance and recovered the active project intent.
- Found the existing Linear project `Grassmann4` and active issue ALOK-749,
  "Close Grassmann.jl port parity gaps in Lean".
- Confirmed the outer git repository is authoritative. The nested
  `Grassmann4/.git` remains on its old `main` index and reports committed outer
  work as dirty; do not commit through it.
- Created branch `codex/alok-749-lean4-port` from the current outer working tree,
  preserving all uncommitted application experiments.
- Current dirty work includes Unreal exports/headers, physics, Metal, SciLean AD,
  curve shortening, collision/constraint, and optimizer experiments plus Lake
  metadata/toolchain drift. None has yet been accepted as supported.
- Identified a concrete correctness problem in the current Unreal FFI: motor
  composition and transforms use explicitly simplified formulas instead of the
  packed `MV PGA3` implementation, while the header declares runtime functions
  that are not visibly implemented. This is a primary repair target.
- Baseline build/test/performance and Julia-parity audits are running read-only.

NEXT

1. Reconcile the canonical Lake configuration and isolate the supported runtime
   import graph from proof/application experiments.
2. Replace the placeholder FFI with packed-kernel-backed exports and add a native
   C smoke test.
3. Run the remaining sparse/truncated/repr/stress/Julia-oracle gates.
4. Continue repairing semantic parity gaps and document the deliberately deferred
   Julia surface.

2026-07-09 NATIVE BACKEND AND CORRECTNESS CHECKPOINT

- `943e84e` replaces the stale SciLean array dependency in the packed runtime
  with Lean's unboxed `FloatArray`. `lake build Grassmann`, `Grassmann.MV`, and
  `Grassmann.DataArray` pass.
- `f5359f9` routes sparse contraction notation to the canonical implementation;
  its independent grade/sign regression suite passes.
- `7f776ed` makes the raw `MV` constructor private, adds checked native-buffer
  import, and makes the default property runner fail nonzero on red results.
- `d33a37c` adds straight-line R3 quaternion and PGA3 dual-quaternion products.
  Exhaustive fixed-kernel tests, direct/typeclass dispatch tests, and the full
  R3/PGA3/CGA3 packed-reference suite pass.
- Release guard after specialization: rotor 127.19 ns/iter (301.8x dense),
  sandwich 98.39 ns/iter (1206.3x), PGA3 motor 453.10 ns/iter, with exact-zero
  correctness diffs.
- `ebdd0cf` replaces the misleading general inverse contract with a checked
  `versorInv?`, deprecates the unsafe old names, and covers the non-scalar
  reverse-product counterexample.
- The remaining binding prototype is still unsupported. A versioned, Lean-free
  C ABI over the canonical packed PGA implementation is now the active slice.

2026-07-09 LEAN 4 PORT COMPLETION

- `acfff42` isolates the packed runtime from dense/proof infrastructure;
  `b3fd53f` makes the outer workspace canonical and reproducible with exact
  mathlib pin `32d24245c7a12ded17325299fd41d412022cd3fe` and no required path
  dependencies.
- `06a9822` shares one Init-only PGA3 kernel between packed `MV`, standalone
  packed PGA constructors/transforms, and foreign callers. Exhaustive basis-pair
  tests cover even-even, even-odd, and odd-even products plus independent
  right-handed rotation/translation anchors.
- `38fef93` exposes the versioned, caller-owned C ABI. The final smoke passed C
  and C++ headers, symbol audit, process and foreign-thread initialization,
  transforms, layout checks, and 100,000 iterations / 500,000 consuming calls.
- `41fcaf6` makes `import Grassmann` the proof-free public runtime and provides
  `Grassmann.Reference` / `Grassmann.All` as explicit dense/extended opt-ins.
- `9608f6a` makes the Julia oracle checkout-independent and safe across the
  historical project's one-time optional-package precompile failure.
- Final correctness gates passed: canonical root build, `Grassmann.Reference`,
  fixed kernels, fat dot, all focused property modes (packed, sparse,
  truncated, representation, stress, dispatch, PGA point cloud), and 161/161
  Julia oracle checks.
- Final benchmark guards passed with zero checked drift: packed rotor
  122.529580 ns (310.9x dense), sandwich 99.087500 ns (1157.6x), PGA3 motor
  438.934170 ns, gradient 830.090420 ns (10.0x finite difference), and PGA3
  motor-point 4340.383400 ns (95.0x dense).
- Final C ABI boundary timing was 14.2 ns/call for construction/copy and
  18.8 ns/call for packed input/extract/copy; ownership stress took 18.3 ms.
- Experimental physics, Metal, Unreal, SciLean-AD, curve-shortening, and local
  nested-workspace edits remain deliberately untouched and outside the
  supported runtime surface.

2026-07-09 ALOK-762 CONTINUATION STARTED

- User requested that the port loop continue after the supported core milestone.
- Three independent audits compared the next soundness, Julia-parity, and
  native-performance slices. The active milestone combines the two coherent
  PGA3 boundary gaps: fused/batched transforms and checked rigid-motor
  normalization/inversion.
- Created high-priority Linear issue ALOK-762, "Ship fused PGA3 transforms and
  checked C ABI v1.1", and branch `codex/alok-762-pga3-abi-v1-1`.
- Fresh public packed baseline: 4326.7375 ns/point versus 421677.2208 ns/point
  dense (97.46x). The required improvement is at least 3x from that packed
  baseline, with a 1500 ns/point ceiling.
- The later soundness milestone is an axiom firewall for the dense/reference
  computational core: remove its dependency on the fake `Ring Float` and
  `Field Float` instances before attempting the currently false or underspecified
  theorem statements.

NEXT

1. Add a scalarized fused odd sandwich and exhaustive independent regressions.
2. Specialize public packed PGA3 transforms and share constructors/extraction.
3. Add checked motor validation, normalization, and inverse.
4. Add contiguous XYZ batch transforms and ABI v1.1 bindings.
5. Run the complete acceptance matrix and record measured results.

2026-07-09 ALOK-762 PGA3 ABI V1.1 COMPLETE

- `26171d8` fuses the fixed PGA3 even-odd-even sandwich into one scalarized
  kernel allocation and routes the public packed odd path through it. Exhaustive
  basis-pair and mixed-coefficient tests agree exactly with the independently
  composed kernels.
- `7991f1d` adds scale-invariant Study-condition validation, normalization,
  checked inverse, shared point/plane/line/rotor/translator constructors, and a
  flat XYZ batch transform that hoists one normalized affine transform per
  cloud. Adversarial NaN, infinity, short/long storage, null, Study-invalid,
  reciprocal-overflow, scale, inverse, and round-trip cases pass.
- `1533540` and `dfe4ab3` release those operations through the caller-owned C
  ABI v1.1 (`0x00010001`) while retaining the `_v1` symbol family and exposing
  no Lean heap objects. Empty/null, in-place, bad-length, invalid-motor,
  overflow, foreign-thread, C/C++ header, and ownership paths all pass.
- `9dfc754` guards the dedicated Lean flat-batch path; `a929571` extends the
  portable Grassmann.jl oracle with packed-order checked-motor comparisons.
- Canonical `lake build` passed 483 jobs; `lake build Grassmann.Reference`
  passed 619 jobs with only pre-existing experimental/reference lint warnings.
  The fixed kernels, fat dot, and Init-only PGA3 regressions also pass.
- All 13 focused property modes passed: 274 reported groups and 19,599 cases
  across packed, sparse, truncated, representation, stress, native/blade
  reference, sign tables, dispatch, rotor exponential, Julia examples, and
  PGA/CGA point clouds. The Julia oracle passed all 188/188 comparisons,
  including the new 27/27 checked-motor block.
- The C ABI smoke completed 100,000 ownership-stress iterations and 900,000
  consuming calls in 169.3 ms. A 4,096-point C batch took 32.0 us versus
  311.6 us through repeated scalar calls (9.73x); construction/copy measured
  13.6 ns and packed input/extract/copy measured 19.2 ns.
- The public packed point transform improved from the fresh 4326.7375 ns/point
  baseline to 542.3333 ns/point (7.98x faster and below the 1500 ns ceiling),
  versus 425211.4041 ns/point dense (784.04x). The Lean flat batch measured
  10.529277 ns/point versus 660.720620 ns/point scalar (62.75x).
- Existing physics, Metal, Unreal, SciLean-AD, curve-shortening, and nested
  workspace edits remain untouched and outside this milestone.

NEXT

1. Firewall the dense/reference computational surface from axiom-backed Float
   algebra instances.
2. Keep exact-law generic algebra separate from lawless IEEE-754 execution and
   add a negative `Ring Float` synthesis regression.
3. Re-run the full reference/property/oracle matrix before widening the
   supported opt-in surface.

2026-07-09 ALOK-763 REFERENCE AXIOM FIREWALL STARTED

- Closed ALOK-762 in Linear after commit `24c0360` and moved to branch
  `codex/alok-763-reference-axiom-firewall`.
- Created high-priority Linear issue ALOK-763, "Firewall dense Float execution
  from algebraic axioms".
- The Reference closure contains 47 local modules. Twenty-seven currently have
  a transitive path to `Grassmann.Proof`, but the actual custom-axiom payload is
  concentrated in the fake Float Ring/Field instance and five `Repr` theorem
  drafts.
- A read-only `/tmp` prototype already passes `lake build Grassmann.Reference`
  (616 jobs) with a lawless coefficient-operations class, no fake Float
  Ring/Field, and no transitive Proof import. Dense Float computation still
  returns the expected `e1 * e1 = 1.0`.
- The prototype found one non-obvious operational requirement: generic PGA/CGA
  formulas use `(2 : F)`, so the capability must include `OfNat F 2` alongside
  zero, one, addition, subtraction, negation, and multiplication.
- The exact Float counterexample `(1e20 + -1e20) + 3 = 3` while
  `1e20 + (-1e20 + 3) = 0` will guard why exact associativity must not return.

NEXT

1. Land the coefficient-operations boundary and migrate computational scopes.
2. Isolate representation theorem drafts and remove Proof imports/instances.
3. Add negative synthesis/import-graph regressions and exact-ring checks.
4. Run the complete acceptance matrix and record exact results.

2026-07-09 ALOK-763 REFERENCE AXIOM FIREWALL COMPLETE

- `31b41e9` introduces `CoeffOps`, a lawless bundle of zero, one, literal two,
  addition, subtraction, negation, and multiplication. Dense, sparse,
  truncated, PGA/CGA, calculus, linear algebra, sign-table, blade-index, and
  storage computation now requires executable operations rather than false
  algebraic laws; operations-only `GAlgebra` instances remain available for
  Float.
- `e61cbf6` removes the fake Float Ring/Field and all 36 axiom-backed law
  fields. `Grassmann.Reference` no longer reaches `Grassmann.Proof`; the five
  underspecified arbitrary-`BEq` representation theorem drafts moved to the
  explicit `Grassmann.ReprTheorems` proof-only module.
- `068dca4` adds a compile-time firewall. In one Reference import it verifies
  that `Ring Float`, `Field Float`, `sorryProofAxiom`, and `sorryDataAxiom` are
  unavailable while native Float operations, `CoeffOps Float`, dense/sparse/
  truncated Float `GAlgebra`, exact `Int` rings, conversions, and products all
  remain available. It also locks in a concrete Float non-associativity witness.
- `19dc484` removes the same unnecessary Proof dependency from legacy packed
  even kernels; `f2b6e0a` removes stale guidance that Float evaluation needs
  proof axioms; `2b3c46c` migrates the committed static-optimizer kernel scope.
- The Reference import walk still reaches 47 local modules, but reaches neither
  `Proof` nor `ReprTheorems`, and no reachable source contains an axiom,
  `sorry`, `sorry_proof`, or `sorry_data` payload.
- Canonical `lake build` passed 483 jobs. The combined Reference firewall,
  explicit theorem-draft, and legacy-even build passed 623 jobs. All 13 focused
  property modes passed 274 groups / 19,599 cases against a freshly rebuilt
  executable. Fixed kernels, fat dot, and Init-only PGA3 regressions passed.
- The portable Julia oracle passed 188/188. C ABI v1.1 smoke passed with
  100,000 ownership iterations / 900,000 consuming calls in 169.9 ms; its
  4,096-point batch took 32.2 us versus 296.0 us scalar (9.19x).
- Performance guards retained zero checked drift: packed rotor 125.279580 ns
  (305.3x dense), sandwich 103.989580 ns (1146.1x), motor 446.774580 ns, and
  analytic gradient 744.347080 ns (7.9x finite difference). Packed PGA3 point
  transform measured 548.308400 ns (649.1x dense); the flat Lean batch measured
  10.605123 ns/point versus 679.222168 ns/point scalar (64.05x).
- A kitchen-sink `Grassmann.All` audit compiled the long Stress/DSL/oracle
  modules but remains intentionally unsupported for two pre-existing reasons:
  CoffeeshopExamples requires the disabled LeanPlot dependency, and Alok's
  dirty CurveShortening experiment references missing `Grassmann.MV.sub`.
- Alok's uncommitted optimizer derivative additions also needed five local
  `[CoeffOps F]` substitutions to run the full working-tree test executable.
  Those substitutions remain with the existing uncommitted optimizer work;
  none of that experiment was swept into these commits.

NEXT

1. Repair or narrow the kitchen-sink `Grassmann.All` aggregation without
   reintroducing disabled dependencies or absorbing application experiments.
2. Replace false and underspecified theorem statements with honest exact-scalar
   formulations before attempting proofs.
3. Continue draining explicit proof placeholders in bounded, executable slices.

2026-07-09 ALOK-765 PACKED SUBTRACTION AND BROAD IMPORT STARTED

- Closed ALOK-763 in Linear after commit `f11f5d2` and moved to branch
  `codex/alok-765-packed-sub-all`.
- Created high-priority Linear issue ALOK-765, "Add packed subtraction and make
  the broad Lean import buildable".
- The post-firewall kitchen-sink audit found one clean-tree blocker:
  `CoffeeshopExamples` unconditionally imports LeanPlot even though the
  canonical package deliberately disables that dependency. The exact committed
  CurveShortening module builds; Alok's dirty packed rewrite separately exposes
  the missing `Grassmann.MV.sub` primitive.
- Packed `MV` already had addition and negation but no subtraction function or
  `Sub` instance. The active design is a same-parity, one-native-buffer
  coefficient loop, not `add a (neg b)`.
- `Grassmann.All` will remain broad over dependency-free Reference and
  validation modules. CurveShortening and LeanPlot-bound application modules
  stay explicitly importable so unrelated experiments cannot break the
  canonical aggregate.

NEXT

1. Land one-pass packed subtraction and standard dispatch.
2. Add independent parity/layout/dense regressions.
3. Repair the All aggregation and rebuild the dirty curve without staging it.
4. Run performance and acceptance gates, then record exact results.

2026-07-09 ALOK-765 PACKED SUBTRACTION AND BROAD IMPORT COMPLETE

- `822d3a0` adds parity-preserving `MV.sub` over borrowed inputs and the
  standard homogeneous `Sub` instance. `58f24e8` tightens its builder to a
  structurally terminating tail loop; generated C contains one capacity-sized
  `FloatArray`, a direct `goto` loop, two native reads, one subtraction, and one
  push per coefficient, with no call to `MV.add` or `MV.neg`.
- `116fe87` adds exact R2-odd, R3-full, PGA3-even, and CGA3-odd layout checks,
  plus direct/operator/dense agreement in every R3, PGA3, and CGA3
  full/even/odd random linear-operation group. All nine subtraction-bearing
  groups passed 100 generated cases each.
- `978d265` makes `Grassmann.All` the canonical Reference and validation
  aggregate. CurveShortening, CoffeeshopExamples, and LeanPlotDemo remain
  explicit application imports, so the supported root does not acquire a
  broken or maintainer-local LeanPlot dependency.
- `58f24e8` also adds a permanent CGA3 full subtraction benchmark and guard.
  The final guard measured 89.908340 ns direct versus 624.512920 ns for the
  old add-neg composition, a 6.9x speedup, under the 500 ns ceiling and above
  the 3x threshold.
- `509becb` and `b7244e6` document the supported import layers, broad build,
  explicit experiments, and honest standalone status of clean CurveShortening.
- Canonical `lake build` passed 483 jobs. The combined Reference soundness,
  theorem-draft, exact arithmetic, dirty CurveShortening, and All build passed
  632 jobs. The full runtime suite passed 302/302 property groups and
  20,951/20,951 cases, then 6/6 optimization categories and 600/600 cases.
- Fixed packed kernels, fat dot, and the Init-only PGA3 kernels passed. The
  main performance guard retained zero rotor/sandwich drift: packed rotor
  123.482500 ns (304.9x dense), sandwich 97.785000 ns (1174.9x), motor
  444.590420 ns, and analytic gradient 737.722080 ns (7.7x finite difference).
- C ABI v1.1 smoke, public symbols, foreign-thread use, and 100,000-iteration
  ownership stress passed. The 4,096-point C batch took 32.5 us versus 295.6 us
  scalar (9.10x); boundary copies measured 13.2 ns and 18.7 ns.
- All unrelated dirty curve, optimizer, physics, Metal, Unreal, SciLean-AD,
  nested-workspace, and Lake edits remain unstaged and uncommitted.

NEXT

1. Replace boxed-intermediate packed add, negation, and scalar multiplication
   with borrowed single-buffer loops and put them under the same benchmark
   discipline as subtraction.
2. Fill the lawless packed API gaps for `Zero`, `SMul Float`, and `Inhabited`
   without inventing an invalid odd-parity multiplicative identity.
3. Continue auditing supported callers for Julia-port API gaps before widening
   application or optional-dependency surfaces.

2026-07-09 ALOK-766 PACKED LINEAR ARITHMETIC STARTED

- Closed ALOK-765 in Linear after commit `42083b1` and moved to branch
  `codex/alok-766-packed-linear-arithmetic`.
- Created high-priority Linear issue ALOK-766, "Make packed linear arithmetic
  allocation-tight and complete".
- `MV.add`, `MV.neg`, and `MV.smul` still use `Array.range`, boxed
  `Array Float`, and a copy into `FloatArray`. Subtraction now demonstrates the
  intended borrowed, monomorphic tail-loop shape and permanent guard pattern.
- The core also exposes functions for zero and even identity but lacks standard
  `Zero`, `SMul Float`, and `Inhabited` instances. Identity must remain
  parity-sensitive: even and full are valid, odd is not.

NEXT

1. Measure the current boxed baselines and audit generated C and instance
   boundaries.
2. Land direct add/neg/smul loops and exact API regressions atomically.
3. Extend permanent benchmarks and run the complete acceptance matrix.

2026-07-09 ALOK-766 PACKED LINEAR ARITHMETIC COMPLETE

- `ec7fb6f` replaced boxed `Array.range`/`Array.map` add, negation, and Float
  scalar multiplication with borrowed-input, monomorphic tail loops that build
  one capacity-sized `FloatArray`.
- `923e5a8` added `Zero`, `Inhabited`, and `SMul Float` for every packed parity,
  plus representable `One` instances for even and full storage only.
- `ef54bca` expanded all nine randomized R3/PGA3/CGA3 full/even/odd linear
  groups and exact layout/instance checks, including rejection of odd `One`.
- `e2a875c` added a permanent 32-coefficient CGA3 boxed-vs-direct benchmark and
  thresholded JSON guard. `d4bac9f` added a generated-C gate for one output
  allocation, direct unboxed arithmetic, borrowed inputs, tail jumps, and the
  unboxed `double` smul ABI.
- `eef0310` documented the runtime interfaces and repeatable evidence.
  `f8d5dc9` routed packed commutator and Spinor subtraction through direct
  `MV.sub`, added the missing Spinor linear instances, and pinned their notation.
- Acceptance passed: explicit broad build 632 jobs, canonical build 483 jobs,
  post-cleanup `Grassmann.All` 625 jobs, 302/302 property groups, 600/600
  optimization cases, all three fixed-kernel executables, generated-C audit,
  C ABI v1.1 smoke, and both performance guards.
- The final packed guard measured add 86.842000 ns (3.760x boxed), negation
  82.148416 ns (3.651x), smul 84.374832 ns (3.578x), subtraction 91.032500 ns
  (1.783x optimized add-neg), and PGA3 motor-point transforms 653.129x dense,
  all with zero checked drift.
- Linear ALOK-766 received the exact commit/build/property/kernel/codegen/ABI/
  benchmark evidence and was moved to Done.

NEXT

1. Special-case full-storage packed-index decoding, which is identity in every
   valid kernel loop and currently rebuilds or consults an unnecessary map.
2. Rewrite reverse, involute, and conjugate as borrowed one-buffer kernels with
   parity-aware zero-allocation fast paths where the operation is identity.
3. Add deterministic all-slot unary/projector regressions before optimizing the
   remaining Hodge, grade-projection, parity-projection, and widening paths.

2026-07-09 ALOK-767 PACKED UNARY KERNELS STARTED

- Closed ALOK-766 in Linear after commit `18c349f` and moved to branch
  `codex/alok-767-packed-unary`.
- Created high-priority Linear issue ALOK-767, "Optimize packed index decoding
  and unary involutions".
- Read-only source and native prototype audits found boxed
  `Array.range`/`Array.map` paths in reverse, involute, conjugate, Hodge dual,
  projectors, grade projection, widening, and dense conversion.
- The bounded first target is reverse/involute/conjugate. CGA3 native prototypes
  were roughly 31x-43x faster for nontrivial unary loops; even involution can
  return its immutable input without allocating, and odd involution can use the
  new negation kernel.
- `unpackIdx` also consults a parity-index array for full storage even though
  full packing is identity. A monomorphic CGA3 full multiplication prototype
  improved from about 4.18 us to 3.05 us when that lookup was removed.

NEXT

1. Land and exactly guard the valid full-storage identity decode.
2. Strengthen packed buffer/all-slot unary regressions before rewriting kernels.
3. Land borrowed unary loops, generated-C gates, and permanent benchmarks.

2026-07-09 ALOK-767 PACKED UNARY KERNELS COMPLETE

- `442828d` makes valid full-storage unpacking the direct blade-mask identity.
  `52b8583` adds deterministic all-slot CGA3 full/even/odd checks, the R2 odd
  curve layout, buffer invariants, involution laws, and public wrapper dispatch.
- `5eb50b7` replaces boxed range/map reverse and Clifford conjugation with
  borrowed one-buffer tail loops. Full grade involution uses the same shape,
  even involution retains and returns its immutable input, and odd involution
  delegates to the optimized negation loop.
- `e097474` adds permanent 100,000-iteration CGA3 unary comparisons and strict
  layout-specific thresholds. All nine preflights had zero L1 drift. Direct
  full operations measured 327.273750-395.760000 ns, parity reverse/conjugation
  204.964580-216.921250 ns, even involution 20.438340 ns, and odd involution
  51.632500 ns; speedups over the retained boxed shapes were 33.439x-351.206x.
- `c006ddb` pins the generated-C loop shape for all five unary helpers and the
  three public involution branches. `532a08c` documents the runtime contract,
  thresholds, structural checks, and measured results.
- Canonical build passed 483 jobs; `Grassmann.All` passed 626; dirty
  CurveShortening passed 478; the explicit Reference/soundness/theorem/All
  matrix passed 633. The full runtime suite passed 302/302 named groups and
  20,951 cases, followed by all 6 optimization categories and 600 cases.
- Fixed packed kernels, fat dot, Init-only PGA3 kernels, and the 12-example
  Julia visual/numeric oracle passed. All 40 numeric witnesses had zero observed
  error and the maximum visual normalized RMSE was 0.15394 below the 0.25 gate.
- C ABI v1.1 smoke passed with 100,000 ownership iterations / 900,000 consuming
  calls in 170.6 ms. Boundary copies were 13.5 ns and 18.9 ns; the 4,096-point
  batch took 31.6 us versus 301.8 us scalar, a 9.56x speedup.
- The core guard retained zero checked drift: rotor 121.700410 ns (315.3x
  dense), sandwich 98.163750 ns (1170.5x), motor 429.302080 ns, and analytic
  gradient 732.027090 ns (7.9x finite difference). The packed guard retained
  the linear-kernel thresholds and measured the PGA3 point path at 641.385x
  dense.
- All unrelated dirty curve, optimizer, physics, Metal, Unreal, SciLean-AD,
  nested-workspace, and Lake edits remain unstaged and uncommitted.

NEXT

1. Replace boxed full-storage Hodge dual with a borrowed one-buffer kernel using
   the existing cached sign table and complementary blade index.
2. Give Hodge its own deterministic all-slot and named randomized oracle checks,
   then pin timing and generated-C structure.
3. Continue with grade/parity projection, widening, and dense-conversion loops.

2026-07-09 ALOK-768 PACKED HODGE DUAL STARTED

- Closed ALOK-767 in Linear after commit `8b798b2` and moved to branch
  `codex/alok-768-packed-hodge`.
- Created high-priority Linear issue ALOK-768, "Make packed Hodge dual
  table-driven and allocation-tight".
- Current `MV.hodgeDual` still constructs `Array.range`, maps boxed Float
  coefficients through per-element Blade/BitVec complement work, and copies the
  result into `FloatArray`.
- Native CGA3 prototypes measured about 23.4 us for the boxed path and 22.9 us
  for a one-buffer path that recomputed complement signs. Reusing the existing
  cached sign table reduced the same all-32-coefficient operation to about
  127 ns, roughly 184x faster than the current implementation.
- Hodge remains intentionally full-only because it flips parity in odd
  dimensions. The implementation must preserve a correct uncached-signature
  fallback rather than specializing the public semantics to CGA3.
- Source and compiler audits established that complementary input/output masks
  make Hodge's sign independent of metric and degeneracy. The active design
  packs all signs for dimensions 0-6 into one unboxed `UInt64`; dimension 7 and
  above use direct permutation parity. This is smaller and faster than a 4^n
  geometric sign table while retaining PGA3's nonzero null-blade complements.

NEXT

1. Pin exact all-slot and standard-signature semantics before the rewrite.
2. Land cached-table and direct-fallback one-buffer loops.
3. Add timing/codegen gates, then run the complete acceptance matrix.

2026-07-09 ALOK-768 PACKED HODGE DUAL COMPLETE

- `408ced4` isolates packed Hodge semantics in a focused exact module;
  `c2f4de9` covers dimensions 0 and 1, standard R2/R3/R4/STA/PGA3/CGA3
  signatures, and the dimension-6/dimension-7 dispatch boundary.
- `f29c927` replaces the boxed range/map/copy implementation with a borrowed,
  one-buffer kernel. Dimensions 0-6 consume an unboxed `UInt64` orientation
  stream; dimensions 7 and above use direct `parityJoinBasic`. Degenerate PGA3
  retains the exterior left-complement anchors `e4 -> -e123` and `e123 -> e4`.
- `7447270` adds the permanent boxed-vs-direct Hodge benchmark and zero-drift
  preflight. `bedd2b1` makes C-facet generation cache-independent and pins the
  selector ABI, both one-read/one-push tail loops, and the single-allocation
  public dispatch. `4008405` documents the runtime semantics and thresholds.
- A post-clean ABI run exposed that Lake could fetch only export-facet trace
  metadata after a clean build. `cbfb075` disables the shared artifact cache for
  those exact native facets, so the C binding smoke always links current local
  `PGA3Kernel` and `CABI` objects.
- Canonical root build passed 483 jobs; dirty CurveShortening passed 478 with
  only its existing long-line warnings; the explicit Reference/soundness/
  theorem/arithmetic/curve/All matrix passed 634 jobs.
- The complete property executable passed 305/305 named groups, including the
  three separately named 100-case packed Hodge comparisons, followed by all 6
  optimization-consistency categories and 600 cases. Fixed packed kernels,
  fat-dot, and Init-only PGA3 kernel regressions passed.
- The Grassmann.jl oracle passed 188/188 numeric and example witnesses. All
  observed algebraic comparison errors stayed within their gates.
- C ABI v1.1 passed C/C++ headers, public symbols, runtime, pthread ownership,
  and layout checks. Boundary copies measured 12.8 ns and 18.7 ns; 100,000
  ownership iterations / 900,000 consuming calls took 163.2 ms; the 4,096-point
  batch took 32.1 us versus 288.0 us scalar (`8.98x`).
- The isolated core guard retained zero rotor/sandwich drift: rotor
  `119.645000 ns` (`308.9x` dense), sandwich `96.867080 ns` (`1166.1x`), motor
  `416.905410 ns`, and analytic gradient `721.077920 ns` (`7.8x` finite
  difference).
- The final packed guard had zero checked drift. Hodge measured
  `77.165840 ns/iter` direct versus `26675.972920 ns/iter` boxed (`345.697x`).
  The PGA3 point path was `643.978x` dense; add/neg/smul were
  `3.812x`/`3.543x`/`3.733x`, direct subtraction was `1.764x`, and every unary
  involution remained above its threshold.
- All unrelated dirty curve, optimizer, physics, Metal, Unreal, SciLean-AD,
  nested-workspace, and Lake edits remain unstaged and uncommitted.

NEXT

1. Replace parity-index arrays and linear searches with checked arithmetic
   rank/unrank while preserving packed order and dimension-zero behavior.
2. Rewrite even/odd projection, grade projection, and parity widening as
   borrowed one-buffer loops with exact all-slot laws.
3. Add permanent projection timing/codegen guards, then continue to dense
   ingress and remaining generic product hot paths.

<promise>COMPLETE</promise>

2026-07-09 ALOK-769 PACKED PROJECTIONS AND PARITY INDEXING STARTED

- Closed ALOK-768 in Linear after commit `f02a4b0` and moved to branch
  `codex/alok-769-packed-projections`.
- Created high-priority Linear issue ALOK-769, "Make packed parity indexing and
  projection/widening allocation-tight".
- Source and exhaustive independent audits established that, for dimensions
  above zero, valid even/odd rank is `mask >>> 1` and unrank reconstructs the
  discarded low bit from the packed rank's popcount parity. Public
  `computePackIdx` and `packIdx` deliberately differ for invalid full masks,
  and the one-slot dimension-zero odd layout remains observable through raw
  packed access.
- The five remaining projector/widening operations still built boxed
  `Array Float` intermediates through range/map callbacks before copying into
  `FloatArray`. The target shape is a borrowed source, one capacity-sized result
  buffer, direct get/push tail loops, and explicit dimension-zero compatibility
  branches.

NEXT

1. Land arithmetic rank/unrank and exact boundary/all-slot regressions.
2. Replace the five boxed projection/widening bodies with one-buffer kernels.
3. Add measured timing and generated-C gates, then run full acceptance.

2026-07-09 ALOK-769 PACKED PROJECTIONS AND PARITY INDEXING COMPLETE

- `c64f5e9` removes cached pack maps, adds checked arithmetic rank/unrank,
  routes valid core and dense callers through it, and replaces even/odd part,
  grade projection, and parity widening with borrowed one-buffer tail loops.
  It also repairs scoped grade notation through carrier-indexed
  `GAGradeProject` and adds exhaustive focused coverage through dimension 12,
  exact dimension-zero/invalid boundaries, all-slot CGA3, and dimension six.
- Initial measurement exposed generic boxed-`Nat` shift/OR as a real hotspot:
  full-to-parity projection was about 1.27 us. `3bfdf5b` uses the equivalent
  `rank + rank + lowBit` formula and reduces the operation to about 164 ns.
  `e4e2034` avoids reconstructing full masks in packed reverse/conjugation and
  derives their signs from one packed-rank popcount, restoring 173--180 ns
  parity unary kernels and more than 40x boxed speedups.
- `8b69e0a` adds seven permanent full/even/odd CGA3 boxed-vs-direct comparisons.
  `86de458` adds zero-drift, absolute-latency, relative-speedup, JSON, and
  environment-override gates. The final thresholded run measured full/even/odd
  grade projection at 332.903330/148.915420/167.370420 ns, even/odd part at
  164.517920/163.958750 ns, and even/odd widening at
  157.757500/164.021250 ns. Every L1 diff was zero and speedups were
  3.209x--12.716x.
- `0c6afac` pins cache-independent arithmetic rank/unrank, one-popcount packed
  unary signs, four allocation-free projection/widening helpers, direct helper
  returns, one-result-buffer public dispatch, and dimension-zero retain/zero
  branches in freshly generated C. `8c034a8` documents the runtime contract,
  thresholds, structural gates, and measured results.
- Canonical `lake build` passed 483 jobs. The explicit Reference, soundness,
  theorem, arithmetic, unary, Hodge, projection, dirty CurveShortening, and All
  matrix passed 683 jobs with only the existing theorem/style/deprecation
  warnings and zero errors.
- The complete property executable passed 305/305 named groups, followed by all
  6 optimization-consistency categories and 600/600 cases. Fixed packed
  kernels covered 1,164 coefficient comparisons; fat dot passed 10 assertions;
  the Init-only PGA3 kernel suite passed 602 named gates / 1,149 primitive
  requirements.
- The Grassmann.jl oracle passed 188/188; its largest displayed absolute
  difference was 0.000019 in one projective-torus plot sample. C ABI v1.1
  passed headers, public symbols, foreign use, 100,000 ownership iterations /
  900,000 consuming calls in 176.2 ms, and a 4,096-point batch at 31.4 us versus
  307.8 us scalar (`9.79x`). Boundary copies measured 13.2 ns and 18.5 ns.
- The core guard retained zero rotor/sandwich drift: rotor 137.979160 ns
  (`283.8x` dense), sandwich 106.628330 ns (`1126.3x`), motor 460.331250 ns,
  and analytic gradient 760.126250 ns (`8.0x` finite difference). The packed
  guard also retained every prior arithmetic, unary, Hodge, and PGA3 threshold.
- All unrelated dirty curve, optimizer, physics, Metal, Unreal, SciLean-AD,
  nested-workspace, and Lake edits remain unstaged and uncommitted.

NEXT

1. Replace boxed `MVDense.ofMultivector` ingress with a borrowed one-buffer
   builder while preserving closure ownership and dimension-zero semantics.
2. Add exact dense-ingress timing/codegen gates and a buffer-native boundary
   audit before changing any public binding ABI.
3. Continue through remaining generic constructor/product hot paths and Julia
   surface gaps under the same correctness/performance discipline.

<promise>COMPLETE</promise>

2026-07-09 ALOK-770 ALLOCATION-TIGHT DENSE INGRESS STARTED

- Closed ALOK-769 in Linear after commit `8ac0e6b` and moved to branch
  `codex/alok-770-dense-ingress`.
- Created high-priority Linear issue ALOK-770, "Make dense-to-packed MV ingress
  allocation-tight".
- Source and generated-code audits established that `MVDense.ofMultivector`
  still built a boxed `Array Float` with `Array.range`/`map`, then copied it
  into `FloatArray`. The target is a borrowed coefficient function, one native
  buffer, proof-erased bounds invariants, and no post-build validation pass.
- The dimension-zero odd layout has one observable compatibility slot even
  though it represents no odd blade, so the rewrite must preserve its exact
  physical buffer and round-trip behavior rather than applying the `n > 0`
  rank formula blindly.

NEXT

1. Land proof-carrying native-buffer construction and one-buffer full/packed
   ingress helpers.
2. Add exhaustive independent conversion fixtures and retained boxed timing
   baselines.
3. Pin generated C, run the complete acceptance matrix, and continue through
   the remaining constructor/product and binding gaps.

2026-07-09 ALOK-770 ALLOCATION-TIGHT DENSE INGRESS COMPLETE

- `3df6520` adds public valid-rank bounds, a proof-carrying `MV.ofDataArray`,
  and borrowed full/packed one-buffer ingress helpers whose size and index
  proofs erase runtime bounds checks and fallback validation. Independent
  tests cover dimensions 0, 1, 5, 6, and 12 across full/even/odd storage,
  including the exact dimension-zero odd compatibility slot.
- `ba2a9d2` adds retained boxed-vs-direct CGA3 full/even/odd benchmarks with 16
  varied coefficient closures and exhaustive physical-data preflight.
  `edf5932` pins freshly generated C to one allocation, one push per output,
  arithmetic packed decoding, tail recursion, and direct helper returns.
- `685708c` integrates dense ingress into the thresholded packed runtime gate;
  `92bc729` records the buffer, ownership, command, and threshold contracts.
  The final run measured full `6337.973750` vs `6518.802090 ns/iter`
  (`1.029x`), even `3294.365420` vs `3386.238750` (`1.028x`), and odd
  `3317.837500` vs `3427.283750` (`1.033x`), with every L1 diff exactly zero.
- Focused MVDense builds passed 584 jobs. Canonical `lake build` passed 483
  jobs; `Grassmann.All` passed 629; dirty `Grassmann.CurveShortening` passed
  478 with only its three existing long-line warnings. Milestone and worktree
  `git diff --check` both passed.
- The complete property executable passed 305/305 named groups followed by all
  6 optimization-consistency categories and 600/600 cases. Fixed packed
  kernels, fat-dot, and Init-only PGA3 kernel regressions all passed.
- The authoritative outer-root Grassmann.jl run passed 188/188 results. A
  deliberately incorrect nested-cwd invocation exposed the existing oracle
  root assumption (`.../Grassmann4/Grassmann4`); the correctly rooted rerun
  returned real Julia values. The separate visual oracle passed 12 examples,
  all 40 formula witnesses, grayscale palette checks, and a worst normalized
  RMSE of `0.15394` against the `0.25` ceiling.
- C ABI v1.1 passed C/C++ headers, exported symbols, foreign use, 100,000
  ownership iterations / 900,000 consuming calls in 171.3 ms, and a
  4,096-point batch at 31.8 us versus 295.2 us scalar (`9.30x`). Boundary
  copies measured 12.9 ns and 18.2 ns. No public ABI changed.
- The isolated core guard retained zero rotor/sandwich drift: rotor
  `123.501660 ns` (`298.2x` dense), sandwich `103.495000 ns` (`1100.5x`),
  motor `439.678750 ns`, and analytic gradient `737.311660 ns` (`7.9x` finite
  difference). Both generated-C guards passed, including all 33 existing
  packed linear/projection/Hodge checks and all five new ingress checks.
- All unrelated dirty curve, optimizer, physics, Metal, Unreal, SciLean-AD,
  nested-workspace, and Lake edits remain unstaged and uncommitted.

NEXT

1. Audit and replace the highest-cost remaining generic product/constructor
   path with allocation-tight native storage and independent dense semantics.
2. Make the Julia oracle root detection fail fast and preserve its planned
   188-result cardinality on transport failures.
3. Continue the Julia API and C/C++ binding surface audit without destabilizing
   the versioned PGA3 ABI.

<promise>COMPLETE</promise>
