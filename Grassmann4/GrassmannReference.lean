import Grassmann.Reference

/-!
# Grassmann reference library

This root exports the dense representations and extended geometric-algebra API
without pulling the repository's validation suites into downstream builds.
-/
