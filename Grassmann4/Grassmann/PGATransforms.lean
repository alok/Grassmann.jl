/-
  Grassmann/PGATransforms.lean - compatibility import for PGA3 transforms

  Packed `translator3` and `rigidMotor3` now live in the proof-free
  `Grassmann.PGA3Packed` module. This import preserves the historical module
  path for dense/reference callers.
-/
import Grassmann.PGA
