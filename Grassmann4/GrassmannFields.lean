import GrassmannFields.R3
import GrassmannFields.Examples.MixedRotor

/-!
# Reusable multivector-field sampling

`GrassmannFields` is the UI-independent library boundary for sampled `Cl(3, 0)`
fields. Import `GrassmannViz` separately when an InfoView renderer is wanted.
-/
