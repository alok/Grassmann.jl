import Bench.Grassmann.Products

/-!
Grassmann benchmarks (`lake exe bench grassmann`): the generated product kernels at
`Float` against the reference kernels and against Julia (`oracle/bench/grassmann_bench.jl`);
results in docs/PERF.md.
-/
